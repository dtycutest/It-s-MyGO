# 比价平台项目 — 最终对接记录文档

> 最后更新：2026-06-03

---

## 目录

- [一、项目概述与当前进度](#一项目概述与当前进度)
- [二、项目结构总览](#二项目结构总览)
- [三、后端实现详情](#三后端实现详情)
- [四、前端实现详情](#四前端实现详情)
- [五、认证与登录架构](#五认证与登录架构)
- [六、平台数据展示](#六平台数据展示)
- [七、爬虫项目](#七爬虫项目)
- [八、修复记录汇总](#八修复记录汇总)
- [九、未实现功能](#九未实现功能)
- [十、测试结果](#十测试结果)
- [十一、项目启动完整流程（新）](#十一项目启动完整流程)

---

## 一、项目概述与当前进度

### 1.1 项目简介

**项目名称**：「一次买够」网购比价平台

**核心价值**：一次搜索，聚合淘宝、京东等主流电商平台的实时价格信息，提供一站式商品比价服务。

**技术栈**：

| 层级   | 技术                                                       |
| ------ | ---------------------------------------------------------- |
| 前端   | uni-app (Vue 3 + Pinia + TypeScript + SCSS)，编译为微信小程序 |
| 后端   | Python 3.12 + FastAPI + Uvicorn，内存数据层（可切换 MySQL）  |
| 爬虫   | Scrapy + Playwright 真浏览器采集 + MySQL 持久化              |
| 内网穿透 | cpolar（用于真机扫码预览）                                   |

### 1.2 当前进度

**前端**：8 个页面全部完成，Mock 已关闭，100% 走真实 API。

| 页面       | 登录要求   | 状态   |
| ---------- | :--------: | :----: |
| 首页       | 无需       | ✅ 完成 |
| 搜索结果   | 无需       | ✅ 完成 |
| 商品详情   | 无需       | ✅ 完成 |
| 用户中心   | 登录后展示 | ✅ 完成 |
| 收藏列表   | 需登录     | ✅ 完成 |
| 浏览历史   | 需登录     | ✅ 完成 |
| 降价提醒   | 需登录     | ✅ 完成 |
| 登录页     | 登录入口   | ✅ 完成 |

**后端**：全部 33 个 API 接口实现并测试通过，支持微信真实登录 + 模拟登录双入口。

**爬虫**：已采集 194 件商品（京东 106 + 淘宝 123），覆盖 7 个品类，数据存储于 MySQL。

---

## 二、项目结构总览

```
ycg-project/                        # 整合项目根目录
├── backend/                        # Python FastAPI 后端
│   ├── app/
│   │   ├── main.py                 # 所有 API 接口（约 680 行）
│   │   ├── schemas.py              # Pydantic 数据模型定义
│   │   ├── data.py                 # 内存数据存储层
│   │   ├── auth.py                 # Token 签发/校验/刷新
│   │   ├── wechat.py               # 微信 jscode2session 调用
│   │   ├── config.py               # 环境变量配置（MySQL + 微信）
│   │   ├── database.py             # MySQL 连接管理
│   │   ├── models.py               # SQLAlchemy ORM 模型
│   │   ├── seed_loader.py          # 从 MySQL 加载爬虫种子数据
│   │   └── seed.json               # 种子数据本地备份
│   ├── scripts/
│   │   └── import_seed.py          # 种子数据导入脚本
│   ├── tests/
│   │   └── test_api.py             # API 测试用例
│   ├── .env                        # 环境变量（MySQL + 微信密钥）
│   ├── openapi.json                # OpenAPI 3.0 接口契约
│   └── requirements.txt            # Python 依赖
│
├── frontend/                       # uni-app 微信小程序前端
│   ├── src/
│   │   ├── api/
│   │   │   ├── index.ts            # API 封装（19 个接口，含 Mock 开关）
│   │   │   └── types.ts            # TypeScript 类型定义
│   │   ├── stores/
│   │   │   ├── user.ts             # 用户登录状态管理
│   │   │   ├── product.ts          # 商品搜索/详情/推荐
│   │   │   ├── favorite.ts         # 收藏状态
│   │   │   └── priceAlert.ts       # 降价提醒状态
│   │   ├── pages/
│   │   │   ├── index/              # 首页（搜索 + 分类 + 推荐）
│   │   │   ├── searchResults/      # 搜索结果
│   │   │   ├── productDetail/      # 商品详情（平台比价卡片）
│   │   │   ├── userCenter/         # 用户中心（收藏/历史/提醒统计）
│   │   │   ├── favorites/          # 收藏列表
│   │   │   ├── history/            # 浏览历史
│   │   │   ├── priceAlerts/        # 降价提醒
│   │   │   └── login/              # 登录页
│   │   ├── components/
│   │   │   ├── ProductCard.vue     # 商品卡片
│   │   │   ├── SearchBar.vue       # 搜索栏
│   │   │   ├── PlatformBadge.vue   # 平台徽标
│   │   │   ├── PriceLabel.vue      # 价格标签
│   │   │   ├── NoData.vue          # 空数据占位
│   │   │   ├── LoadingIndicator.vue # 加载指示器
│   │   │   └── MoreHeader.vue      # 更多页面头部
│   │   ├── utils/
│   │   │   ├── request.ts          # HTTP 请求封装（拦截器 + 错误处理）
│   │   │   ├── storage.ts          # 本地存储（Token 管理）
│   │   │   └── index.ts            # 通用工具
│   │   ├── mock/                   # Mock 数据（已停用）
│   │   ├── styles/
│   │   │   ├── common.scss         # 全局通用样式
│   │   │   └── variables.scss      # SCSS 变量
│   │   ├── static/                 # 静态资源（logo、TabBar 图标）
│   │   ├── App.vue                 # 根组件
│   │   ├── main.ts                 # 入口文件
│   │   ├── pages.json              # 页面路由 + TabBar 配置
│   │   └── manifest.json           # uni-app 应用配置
│   ├── .env                        # API Base URL（cpolar 公网地址）
│   ├── package.json                # npm 依赖
│   ├── tsconfig.json               # TypeScript 配置
│   └── vite.config.ts              # Vite 构建配置
│
└── crawler/                        # Scrapy 爬虫（独立项目）
    ├── onebuy_crawler/
    │   ├── spiders/                # 4 个爬虫（jd_search/taobao_search/jd_detail/taobao_detail）
    │   ├── pipelines/              # 5 条管道（清洗、去重、日志、图片缓存、MySQL）
    │   ├── middlewares/            # 4 个中间件（UA/代理/重试/Cookie）
    │   ├── services/               # 核心服务（匹配/归一化/任务队列/分类）
    │   └── anti_detect.py          # Playwright 浏览器反检测模块
    ├── jobs/                       # 可运行的任务入口（自动化爬取、数据导入导出等）
    ├── tests/                      # 16 个测试文件
    ├── data/                       # 关键词文件 + 种子数据
    └── browser_profiles/           # 浏览器登录态持久化
```

---

## 三、后端实现详情

### 3.1 运行环境

- **地址**：`http://127.0.0.1:8000`
- **Swagger 文档**：`http://127.0.0.1:8000/docs`
- **ReDoc 文档**：`http://127.0.0.1:8000/redoc`
- **数据**：194 件商品（京东 106 + 淘宝 123），7 个分类
- **启动命令**：
  ```
  cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
  ```

### 3.2 接口清单（全部 33 个）

| 模块     | 方法   | 路径                                | 认证       | 说明                 |
| -------- | ------ | ----------------------------------- | ---------- | -------------------- |
| 监控     | GET    | `/health`                           | 无需       | 健康检查             |
| 认证     | POST   | `/auth/login`                       | 无需       | 微信/simulated 登录  |
| 认证     | POST   | `/auth/refresh`                     | 无需       | 刷新 Token           |
| 认证     | POST   | `/auth/logout`                      | 可选       | 登出                 |
| 商品     | GET    | `/products/search`                  | 可选       | 多条件搜索 + 排序    |
| 商品     | GET    | `/products/{product_id}`            | 无需       | 商品详情             |
| 商品     | GET    | `/products/categories`              | 无需       | 分类树               |
| 用户     | GET    | `/users/profile`                    | 需登录     | 获取用户信息         |
| 用户     | PUT    | `/users/profile`                    | 需登录     | 更新用户信息         |
| 收藏     | GET    | `/users/favorites`                  | 需登录     | 收藏列表             |
| 收藏     | POST   | `/users/favorites`                  | 需登录     | 添加收藏             |
| 收藏     | PUT    | `/users/favorites/{favorite_id}`    | 需登录     | 更新收藏备注         |
| 收藏     | DELETE | `/users/favorites/{favorite_id}`    | 需登录     | 删除收藏             |
| 降价提醒 | GET    | `/users/price-alerts`               | 需登录     | 提醒列表             |
| 降价提醒 | POST   | `/users/price-alerts`               | 需登录     | 创建提醒             |
| 降价提醒 | PUT    | `/users/price-alerts/{alert_id}`    | 需登录     | 更新提醒             |
| 降价提醒 | DELETE | `/users/price-alerts/{alert_id}`    | 需登录     | 删除提醒             |
| 浏览历史 | GET    | `/users/browse-history`             | 需登录     | 历史列表             |
| 浏览历史 | POST   | `/users/browse-history`             | 需登录     | 添加历史（自动去重） |
| 浏览历史 | DELETE | `/users/browse-history`             | 需登录     | 清空历史             |
| 搜索记录 | GET    | `/users/search-records`             | 需登录     | 搜索历史             |
| 热搜词   | GET    | `/search/hot-words`                 | 无需       | 热搜榜               |
| 推荐     | GET    | `/recommendations/personalized`     | 可选       | 个性化推荐            |
| 分析     | POST   | `/analytics/events`                 | 可选       | 行为埋点上报          |
| 爬虫     | GET    | `/crawler/seed`                     | 无需       | 查看种子数据          |
| 爬虫     | GET    | `/crawler/stats`                    | 无需       | 数据统计              |

### 3.3 核心架构设计

**认证流程**：
```
小程序 code → POST /auth/login → jscode2session → openid → 查找/创建用户 → 签发 Token → 前端存储
```

**两种登录模式**：

| 方式         | 可用性   | 说明                                               |
| ------------ | :------: | -------------------------------------------------- |
| 模拟登录     | ✅ 可用   | 传 `code: "mock_dev_code"`，后端直接签发 Token        |
| 真实微信登录 | ✅ 可用   | 调用微信 `jscode2session` API，通过真实 openid 注册/登录 |

**统一响应格式**：
```json
{
  "code": 0,          // 0 = 成功, 非 0 = 业务错误
  "message": "ok",
  "data": { ... }     // 泛型数据体
}
```

**鉴权**：Bearer Token（`Authorization: Bearer <access_token>`），通过 FastAPI `Depends(get_current_user)` 依赖注入。

**数据层**：当前使用内存数据（`data.py`），重启后数据重置。可通过 `seed_loader.py` 从 MySQL 加载爬虫采集的真实商品数据。

### 3.4 推荐算法

基于用户行为（收藏 + 浏览历史）的品类偏好评分：
- 收藏行为权重 +3
- 浏览行为权重 +1
- 优先展示有跨平台比价关系的商品
- 使用 `random.shuffle` + `random.random()` 确保每次换一批具有随机性

### 3.5 搜索算法

四级匹配策略：
1. 关键词精确匹配标题（前缀 +200，近前缀 +100，其他 +80）
2. 关键词去空格匹配标题
3. 多词全部命中或 60% 命中
4. 品类名称匹配
5. 跨平台商品额外 +50 权重

---

## 四、前端实现详情

### 4.1 技术栈

| 组件       | 版本/说明                      |
| ---------- | ------------------------------ |
| 框架       | uni-app 3.0 + Vue 3.4          |
| 状态管理   | Pinia 3.0                      |
| 语言       | TypeScript 4.9                 |
| 样式       | SCSS + rpx 响应式布局          |
| 构建       | Vite 5.2                       |
| 目标平台   | 微信小程序（mp-weixin）        |

### 4.2 页面路由配置

TabBar 底部导航：
- 首页 → `pages/index/index`
- 我的 → `pages/userCenter/userCenter`

子页面（通过 `uni.navigateTo` 跳转）：
- 搜索结果 → `pages/searchResults/searchResults`
- 商品详情 → `pages/productDetail/productDetail`
- 收藏列表 → `pages/favorites/favorites`
- 浏览历史 → `pages/history/history`
- 降价提醒 → `pages/priceAlerts/priceAlerts`
- 登录页 → `pages/login/login`

### 4.3 Pinia Store 状态管理

| Store        | 文件                | 职责                                       |
| ------------ | ------------------- | ------------------------------------------ |
| `useUserStore`   | `stores/user.ts`    | 登录状态、Token 管理、isLoggedIn、用户信息   |
| `useProductStore` | `stores/product.ts` | 搜索、商品详情、热搜词、推荐商品            |
| `useFavoriteStore`| `stores/favorite.ts`| 收藏列表、添加/删除收藏                    |
| `usePriceAlertStore`| `stores/priceAlert.ts`| 降价提醒 CRUD                         |

**用户状态管理（关键逻辑）**：
- `ready` 状态：确保 Token 验证完成后才判定 `isLoggedIn`，防止初始状态下误判为已登录
- `init()` 自动初始化：先验证 Token 有效性再设置登录状态，无效则自动清除

### 4.4 组件清单

| 组件              | 说明                                       |
| ----------------- | ------------------------------------------ |
| `ProductCard`     | 通用商品卡片（图片 + 标题 + 价格 + 平台）   |
| `SearchBar`       | 搜索输入栏（带热搜词下拉）                  |
| `PlatformBadge`   | 平台徽标（淘宝/京东标签）                   |
| `PriceLabel`      | 价格展示标签                               |
| `NoData`          | 空数据占位组件                             |
| `LoadingIndicator`| 加载中指示器                               |
| `MoreHeader`      | 更多页面通用头部                           |

### 4.5 API 封装

文件：`src/api/index.ts`

- `USE_MOCK = false`（已全部切换为真实 API）
- 19 个接口函数分模块封装：authApi、productApi、searchApi、userApi、favoriteApi、priceAlertApi、browseHistoryApi、searchRecordApi、recommendationApi、analyticsApi
- 每个接口支持 Mock/真实双模式

### 4.6 网络请求层

文件：`src/utils/request.ts`

核心特性：
- `BASE_URL` 从 `.env` 文件的 `VITE_API_BASE_URL` 读取（默认 `http://127.0.0.1:8000`）
- 自动附加 `Authorization: Bearer <token>` 请求头
- 401 错误自动清除 Token 并跳转登录页
- 业务错误码映射（401001 → 未登录，404001 → 用户不存在，等）

### 4.7 前端已修复的关键问题

1. **布局不对称**：微信 scroll-view 裁剪右侧 padding → 用内层 `.content-inner` 容器解决
2. **底部遮挡**：底部操作栏遮挡卡片 → 页面和卡片区增加 `padding-bottom`
3. **销量显示异常**："10000000万+" → 添加 `formatSalesVolume` 格式化函数
4. **未知店铺**：无 `seller_name` 仍显示 → 条件渲染 `v-if="platform.seller_name"`
5. **未登录显示退出**：`isLoggedIn` 基于未验证 Token → 增加 `ready` 状态，先验证再设置
6. **浏览历史不更新**：详情页未调用 `browseHistoryApi.add()` → 添加静默记录调用
7. **足迹始终为 0**：`onMounted` 只执行一次 → 增加 `onShow` 在 TabBar 切换时刷新
8. **历史时间不正确**：显示 UTC 时间 → 添加 `formatViewTime` 相对时间格式化
9. **推荐不随机**：排序固定 → 增加 `random.shuffle` + `random.random()` 排序键

---

## 五、认证与登录架构

### 5.1 认证层级

| 接口类型 | 认证方式     | 示例接口                          |
| -------- | ------------ | --------------------------------- |
| 公开浏览 | 无需认证     | 搜索、商品详情、分类、热搜词       |
| 可选认证 | 登录后关联数据 | 分析事件上报、推荐                |
| 用户私有 | 必须认证     | 收藏、降价提醒、浏览历史、个人资料 |

### 5.2 登录流程

**前端（login.vue）**：
- **模拟登录**（保留开发入口）：传 `code: "mock_dev_code"` + 模拟用户信息
- **微信一键登录**：调用 `uni.login()` 获取真实 code → 调用 `uni.getUserInfo()` 获取用户信息 → 发送到后端

**后端（main.py `/auth/login`）**：
- `code == "mock_dev_code"` → 直接签发 Token（演示用户）
- 其他 code → 调用微信 `jscode2session` → 获取 openid → 查找/创建用户 → 签发 Token

### 5.3 前端登录行为

- **公开页面**：无需登录，无任何"未登录"错误提示
- **用户功能页**：未登录时弹出"是否前往登录？"引导框
- **Token 过期**：已登录用户 Token 失效时，自动清除并提示重新登录

---

## 六、平台数据展示

### 6.1 商品详情比价卡片

每个平台的比价卡片展示以下爬虫真实数据：

| 字段     | 数据来源            | 说明                        |
| -------- | ------------------- | --------------------------- |
| 平台名称 | `platform_name`     | 淘宝/京东                   |
| 当前价格 | `price`             | 爬虫采集                    |
| 原价     | `original_price`    | 来自爬虫                    |
| 折扣率   | `discount_rate`     | 计算得出（如 7.5 折）       |
| 销量     | `sales_volume`      | 来自爬虫，已格式化（万+ ）   |
| 店铺名   | `seller_name`       | 来自爬虫（无则不显示）       |
| 店铺评分 | `seller_rating`     | 来自爬虫（⭐ 数值）          |
| 库存     | `stock_quantity`    | 来自爬虫（无销量时兜底）     |
| 是否有货 | `in_stock`          | 绿色"有货"/红色"缺货"标签   |
| 最优价   | 价格 = `min_price`  | "最优价"标签                 |
| 购买链接 | `product_url`       | 复制到剪贴板（小程序不能跳转）|

### 6.2 销量格式化规则

```typescript
const formatSalesVolume = (volume: number): string => {
  if (volume >= 100000000) return `${(volume / 100000000).toFixed(1)}亿+`;
  if (volume >= 10000) return `${(volume / 10000).toFixed(1)}万+`;
  if (volume >= 1000) return `${(volume / 1000).toFixed(1)}千+`;
  return `${volume}件`;
};
```

### 6.3 链接处理

- 微信小程序不支持直接跳转外部电商链接
- 点击平台卡片 → 复制链接到剪贴板 → Toast 提示用户浏览器打开

---

## 七、爬虫项目

### 7.1 架构概览

```
crawler/
├── onebuy_crawler/
│   ├── spiders/          # 4 个爬虫（jd_search/taobao_search/jd_detail/taobao_detail）
│   ├── pipelines/        # 5 条管道（清洗→去重→日志→图片缓存→MySQL 持久化）
│   ├── middlewares/      # 4 个中间件（UA轮换/代理轮换/重试反爬/Cookie注入）
│   ├── services/         # 核心服务（跨平台匹配/归一化/任务队列/分类推断）
│   └── anti_detect.py    # Playwright 浏览器反检测模块
├── jobs/                 # 可运行任务入口
│   ├── automated_crawl.py          # 全自动爬取编排器
│   ├── browser_capture_search.py   # Playwright 真浏览器采集
│   ├── setup_browser_profile.py    # 浏览器登录配置（首次手动登录）
│   ├── import_copied_search_html.py# 京东 HTML 复制导入
│   ├── export_seed_data.py         # 导出种子 JSON
│   ├── import_seed_data.py         # 导入种子 JSON
│   ├── init_schema.py              # 初始化 MySQL 表
│   └── query_products.py           # 查询已采集商品
└── tests/                # 16 个测试文件
```

### 7.2 核心工作流

1. 关键词入队 → `crawl_tasks` 表
2. `browser_capture_search.py` → Playwright 真浏览器打开京东/淘宝 → 模拟搜索 → 提取 DOM 数据
3. 管道链：**清洗 → 去重 → MySQL 持久化**
4. MySQL 自动计算：`min_price`、`max_price`、`price_diff`、`best_platform`

### 7.3 当前数据覆盖

| 维度           | 当前状态            |
| -------------- | ------------------- |
| 平台           | 京东 + 淘宝         |
| 品类           | 7 个大类            |
| 商品数         | 194 件              |
| 平台报价       | 229 条              |
| 价格历史       | 247 条（可绘制趋势图）|
| 采集时间范围   | 2026-05-23 ~ 2026-05-31 |

验收商品列表：
```
图书       余华 活着
纸品       维达 V2239 抽纸 24包
衣物清洁   蓝月亮 深层洁净 洗衣液 3kg
口腔护理   云南白药 益优冰柠牙膏 145g
洗护沐浴   海飞丝 怡神冰凉 洗发水 750ml
洗护沐浴   多芬 深层营润沐浴露 720g
书写工具   晨光 K35 中性笔 0.5mm 12支
碳酸饮料   可口可乐 330ml 24罐
咖啡冲饮   雀巢咖啡 1+2 原味 100条
```

### 7.4 反爬措施

| 层级       | 措施                                                     |
| ---------- | -------------------------------------------------------- |
| 浏览器     | Playwright 真浏览器 + 22 个反检测参数                      |
| 登录态     | `launch_persistent_context` 保存 Cookie + LocalStorage    |
| 反检测模块 | `anti_detect.py`（playwright-stealth + WebGL伪装 + 权限拦截）|
| 请求节奏   | 打字延迟 80ms + 搜索前 600ms + 搜索后 1200ms               |
| 重试策略   | 失败冷却 10min，封禁冷却 45min，最多 3 次                   |
| 故障保护   | 检测到 risk_handler/验证码 → 立即停止本轮                   |

### 7.5 跨平台智能匹配

- **品牌归一化**：Apple/苹果/iPhone → `apple`，覆盖 15+ 品牌
- **品类识别**：区分手机/耳机/键盘/显示器/充电宝等 10+ 品类
- **型号提取**：正则匹配 `iPhone 15 Pro`、`Mate 60`、`Galaxy S24` 等
- **规格指纹**：容量、颜色、键盘轴体等规格特征
- **SHA1 哈希**：生成匹配指纹用于跨平台合并

---

## 八、修复记录汇总

### 8.1 布局与 UI 修复

| 问题                        | 根因                              | 修复方式                            | 文件                     |
| --------------------------- | --------------------------------- | ----------------------------------- | ------------------------ |
| 左右边距不对称              | scroll-view 裁剪右侧 padding     | 内层容器 `.content-inner` 对称 padding | `index.vue`, `searchResults.vue` |
| 底部操作栏遮挡卡片          | 固定底部栏覆盖内容                | 页面和卡片区增加 `padding-bottom`    | `productDetail.vue`      |
| 响应式布局（窄屏标签挤压）  | 价格标签无换行                    | `flex-wrap: wrap` + `flex-shrink: 0` | `searchResults.vue`      |
| 图片轮播超高屏幕异常        | 无最大高度限制                    | `max-height: 60vh`                  | `productDetail.vue`      |
| iPhone 底部安全区适配       | 未考虑 safe-area-inset-bottom     | `padding-bottom: calc(20rpx + env(safe-area-inset-bottom))` | `productDetail.vue` |

### 8.2 数据展示修复

| 问题                        | 根因                              | 修复方式                            | 文件                     |
| --------------------------- | --------------------------------- | ----------------------------------- | ------------------------ |
| "10000000万+" 销量          | 缺少大数字格式化                  | `formatSalesVolume` 函数（亿/万/千/件） | `productDetail.vue`      |
| "未知店铺" 显示             | `seller_name` 为空仍渲染          | 条件渲染 `v-if="platform.seller_name"` | `productDetail.vue`      |
| "足迹"数据始终为 0          | `onMounted` 只在首次加载执行       | 增加 `onShow` 在 TabBar 切换时刷新    | `userCenter.vue`         |
| 浏览历史时间不正确          | 显示 UTC 时间字符串               | `formatViewTime` 相对时间（"X分钟前"）| `history.vue`            |
| 浏览历史列表为空            | 数据字段 `res.data.list` → `items`| 改为 `res.data?.items`              | `history.vue`, `userCenter.vue` |
| 降价提醒无商品图片          | Schema/API 缺少 `product_image`    | Schema + main.py + data.py + 前端类型  | 多文件                    |

### 8.3 认证与状态修复

| 问题                        | 根因                              | 修复方式                            | 文件                     |
| --------------------------- | --------------------------------- | ----------------------------------- | ------------------------ |
| 未登录显示"退出"按钮        | `isLoggedIn` 基于未验证 Token      | 增加 `ready` 状态，先验证再设置       | `stores/user.ts`         |
| 模拟登录传递空参数          | `raw_data`/`signature` 为空字符串  | 改为传递非空 mock 值                 | `login.vue`              |
| 真实微信登录未生效          | `if not raw_data or not signature` | 改为 `if not code` 判断              | `main.py`                |
| `onShow` 编译报错           | 从 `vue` 而非 `@dcloudio/uni-app` 导入 | 改为 `import { onShow } from '@dcloudio/uni-app'` | `userCenter.vue` |

### 8.4 业务逻辑修复

| 问题                        | 根因                              | 修复方式                            | 文件                     |
| --------------------------- | --------------------------------- | ----------------------------------- | ------------------------ |
| 浏览历史不更新              | 详情页未调用 `add()` API          | `getProductDetail` 中静默调用        | `stores/product.ts`      |
| 浏览历史重复保存            | 每次新增不删除旧记录              | 插入前清除同用户同商品旧记录          | `main.py`                |
| 推荐商品不随机              | 排序完全确定性                    | `random.shuffle` + `random.random()` | `main.py`                |
| 热搜词数量少且不准          | 基于 3 条种子搜索记录            | 从商品标题提取高频词 + 品类名          | `main.py`                |
| 搜索记录未保存              | 匿名用户未触发记录                | 所有用户（含匿名）记录搜索词           | `main.py`                |

### 8.5 爬虫修复

| 问题                        | 根因                              | 修复方式                            |
| --------------------------- | --------------------------------- | ----------------------------------- |
| 爬虫启动后 0 pages/0 items  | `async def start()` 非 Scrapy 标准 | 改为 `def start_requests()`         |
| 中间件参数兼容              | 新版 Scrapy 传 `spider` 关键字参数 | 添加 `spider=None` 参数             |
| 浏览器被反爬识别            | Playwright 留有自动化特征          | `anti_detect.py` 统一反检测模块      |

---

## 九、未实现功能（暂不考虑）

| 功能         | 原因                             |
| ------------ | -------------------------------- |
| 价格走势图   | 爬虫无历史价格数据（需定时采集）    |
| 用户评价     | 爬虫不支持评价采集                 |
| 管理后台     | 项目定位为 Demo                  |
| 拼多多爬虫   | 反爬难度高，暂不实施               |
| 综合分排序   | 暂不实现                          |
| 定时自动采集 | 需配置 cron/计划任务              |

---

## 十、测试结果

- ✅ 全部 33 个接口测试通过
- ✅ 10 个完整业务流程全部通过
- ✅ 匿名浏览无"未登录"报错
- ✅ 用户功能页有清晰的登录引导
- ✅ 后端运行在 `http://127.0.0.1:8000`
- ✅ 微信公众号登录接口正常调用
- ✅ 模拟登录入口保留可用
- ✅ 浏览历史正确记录、去重、实时更新
- ✅ 推荐商品每次刷新均产生随机变化

---

## 十一、项目启动完整流程

> 本章节详细说明从零开始启动项目的完整流程，涵盖前端编译、后端运行、内网穿透配置以及真机调试的全部步骤。

### 11.1 环境要求总览

| 组件              | 版本/说明                                     |
| ----------------- | --------------------------------------------- |
| Python            | 3.12+（后端 + 爬虫）                           |
| Node.js           | 18+（前端编译）                                 |
| MySQL             | 8.0+（爬虫数据存储；后端可脱离 MySQL 运行）       |
| 微信开发者工具     | 最新稳定版（[下载页面](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)） |
| cpolar            | 内网穿透工具（[官网下载](https://www.cpolar.com/)） |
| Playwright        | 爬虫浏览器自动化依赖                            |

### 11.2 后端启动

#### 目录位置

```
d:\上课\大三下\计算机综合项目实践\ycg-project\backend\
```

#### 环境变量配置

配置文件：`backend\.env`

```env
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=你的MySQL密码
MYSQL_DATABASE=onebuy
MYSQL_CHARSET=utf8mb4

# 微信公众号配置（需在微信公众平台申请）
WECHAT_APPID=your_wechat_appid
WECHAT_SECRET=your_wechat_secret
```

#### 安装依赖

```powershell
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\backend"
pip install -r requirements.txt
```

依赖清单（`requirements.txt`）：
```
fastapi==0.110.0
uvicorn[standard]==0.27.1
pytest==8.1.1
requests==2.31.0
httpx==0.27.2
sqlalchemy>=2.0.0
pymysql>=1.1.0
python-dotenv>=1.0.0
```

#### 启动服务

```powershell
# 开发模式（带热重载）
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\backend"
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

启动成功标志：
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [...]
```

#### 验证后端

- 浏览器打开 `http://127.0.0.1:8000/docs` 应能看到 Swagger UI 接口文档
- 浏览器打开 `http://127.0.0.1:8000/health` 应返回 `{"code":0,"message":"服务正常","data":"ok"}`

#### 加载种子数据（可选）

如果 MySQL 中有爬虫数据，后端启动时会自动从 MySQL 加载真实商品数据。相关代码在 `app/seed_loader.py` 中，由 `app/data.py` 在初始化时调用。

如果不需要 MySQL 数据，后端使用内置演示数据也能正常运行。

### 11.3 前端启动

#### 目录位置

```
d:\上课\大三下\计算机综合项目实践\ycg-project\frontend\
```

#### 环境变量配置

配置文件：`frontend\.env`

```env
# 后端 API 地址
# 本地开发（微信开发者工具在同一台电脑）：http://127.0.0.1:8000
# 局域网演示（笔记本开热点）：http://192.168.137.1:8000
# 校园网演示：http://10.138.182.232:8000
# cpolar 公网穿透（真机扫码预览用）
VITE_API_BASE_URL=https://39debfb1.r32.cpolar.top
```

> **重要**：如果使用微信开发者工具的"不校验合法域名"模式，可以使用 `http://127.0.0.1:8000`。如果要在真机上扫码预览，必须使用公网 HTTPS 地址（如 cpolar 提供的地址）。

#### 安装依赖

```powershell
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\frontend"
npm install
```

#### 编译为微信小程序

```powershell
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\frontend"
npm run dev:mp-weixin
```

编译成功后输出目录：`frontend/dist/dev/mp-weixin/`

```
DONE  Build complete.
运行方式：打开 微信开发者工具, 导入 dist/dev/mp-weixin 运行。
```

#### 在微信开发者工具中打开

1. 打开 **微信开发者工具**
2. 选择 **导入项目**
3. 项目目录选择：`d:\上课\大三下\计算机综合项目实践\ycg-project\frontend\dist\dev\mp-weixin`
4. AppID 使用自己的小程序 AppID（在微信公众平台申请）或选择"测试号"
5. 点击 **确定** 导入

#### 微信开发者工具设置

1. 点击右上角 **详情** → **本地设置**
2. 勾选 **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**（开发阶段必须开启）
3. 确保 **基础库版本** >= 3.0.0

#### 编译检查（TypeScript 类型检查）

```powershell
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\frontend"
npm run type-check
```

### 11.4 内网穿透配置（cpolar）

内网穿透用于将本地后端服务暴露到公网，使真机扫码预览时能访问后端 API。

#### 安装 cpolar

1. 从 [cpolar 官网](https://www.cpolar.com/) 下载 Windows 版本
2. 安装完成后，注册账号并登录

#### 启动隧道

```powershell
# 将本地 8000 端口的后端服务暴露到公网
cpolar http 8000
```

启动成功后，cpolar 会显示类似以下信息：

```
Forwarding   https://xxxxxxxx.r32.cpolar.top -> http://127.0.0.1:8000
```

#### 更新前端 .env

将 cpolar 提供的 HTTPS 地址填入前端环境变量：

```env
VITE_API_BASE_URL=https://xxxxxxxx.r32.cpolar.top
```

> **当前项目 cpolar 地址**：`https://39debfb1.r32.cpolar.top`（cpolar 每次重启地址可能变化）

#### 更新微信小程序服务器域名（生产环境需要）

如果要在不勾选"不校验域名"的情况下运行，需要在微信公众平台后台配置：

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 开发 → 开发管理 → 开发设置 → 服务器域名
3. 在 **request 合法域名** 中添加 cpolar 地址：`https://xxxxxxxx.r32.cpolar.top`

> 注意：cpolar 免费版地址会变化，每次重启后需要更新。

#### 更新 frontend .env 后重新编译

```powershell
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\frontend"
npm run dev:mp-weixin
```

### 11.5 真机调试流程

1. **确保后端运行**：`python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload`
2. **确保 cpolar 运行**：`cpolar http 8000`
3. **确保 frontend .env 指向 cpolar 地址**
4. **重新编译前端**：`npm run dev:mp-weixin`
5. **微信开发者工具中点击"预览"** → 生成二维码
6. **手机微信扫码** → 真机预览

### 11.6 完整启动命令速查

假设从项目根目录 `d:\上课\大三下\计算机综合项目实践\ycg-project\` 开始：

```powershell
# ========== 终端 1：启动后端 ==========
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\backend"
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload

# ========== 终端 2：启动内网穿透（真机调试时才需要）==========
cpolar http 8000
# 记下输出的 HTTPS 地址，如 https://39debfb1.r32.cpolar.top

# ========== 更新前端 .env ==========
# 编辑 d:\上课\大三下\计算机综合项目实践\ycg-project\frontend\.env
# 将 VITE_API_BASE_URL 改为 cpolar 输出的 HTTPS 地址

# ========== 终端 3：编译前端 ==========
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\frontend"
npm install          # 首次运行需要
npm run dev:mp-weixin

# ========== 微信开发者工具：打开编译产物 ==========
# 导入目录：d:\上课\大三下\计算机综合项目实践\ycg-project\frontend\dist\dev\mp-weixin
```

### 11.7 开发模式说明

#### 仅电脑端开发（不需要真机/内网穿透）

此模式最简单，只需后端 + 前端 + 微信开发者工具：

```
终端 1: cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
终端 2: cd frontend && npm run dev:mp-weixin
微信开发者工具: 导入 dist/dev/mp-weixin，勾选"不校验合法域名"
```

前端 `.env` 中使用：
```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```

#### 局域网演示（笔记本开热点，其他设备连热点）

```
终端 1: cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
终端 2: cd frontend && npm run dev:mp-weixin
微信开发者工具: 导入 dist/dev/mp-weixin，勾选"不校验合法域名"
```

前端 `.env` 中使用笔记本电脑的局域网 IP：
```env
# 在终端运行 ipconfig 查看无线局域网适配器的 IPv4 地址
VITE_API_BASE_URL=http://192.168.xxx.xxx:8000
```

#### 真机扫码预览（需要 cpolar 公网穿透）

```
终端 1: cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
终端 2: cpolar http 8000
终端 3: cd frontend && npm run dev:mp-weixin
微信开发者工具: 点击"预览"，手机微信扫码
```

前端 `.env` 中使用 cpolar 提供的 HTTPS 地址：
```env
VITE_API_BASE_URL=https://39debfb1.r32.cpolar.top
```

### 11.8 爬虫启动（可选，仅需更新商品数据时）

爬虫是独立项目，位于 `ycg-project/crawler/`，详细说明见 [`crawler/README.md`](file:///d:/上课/大三下/计算机综合项目实践/ycg-project/crawler/README.md)。

```powershell
# 进入爬虫目录
cd "d:\上课\大三下\计算机综合项目实践\ycg-project\crawler"

# 首次配置（创建虚拟环境 + 安装依赖 + 安装 Chromium）
py -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
.\.venv\Scripts\python.exe -m playwright install chromium

# 配置 MySQL 连接（复制 .env.example 为 .env 并填写密码）
copy .env.example .env

# 初始化数据库表
.\.venv\Scripts\python.exe -m jobs.init_schema

# 首次使用前手动登录电商平台（保存 Cookie）
.\.venv\Scripts\python.exe -m jobs.setup_browser_profile --platform all

# 导入已有的种子数据
.\.venv\Scripts\python.exe -m jobs.import_seed_data --seed-file data\onebuy_seed_records_all.json --platform all

# 查询已采集的商品
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "余华 活着" --platform all
```

### 11.9 故障排查

| 现象                           | 可能原因                        | 解决方法                                          |
| ------------------------------ | ------------------------------- | ------------------------------------------------- |
| 前端请求失败、无数据            | 后端未启动                      | 检查终端 1 是否正常运行 uvicorn                    |
| 前端提示"网络请求失败"          | .env 中的 BASE_URL 不正确       | 检查 `frontend\.env` 中的 `VITE_API_BASE_URL`      |
| 真机扫码后白屏/无数据           | cpolar 未启动或地址过期          | 重启 cpolar，更新 .env 中的地址，重新编译前端       |
| 微信开发者工具提示"不在合法域名" | 未勾选"不校验合法域名"          | 详情 → 本地设置 → 勾选"不校验合法域名"             |
| 登录失败/Token 无效             | 微信 AppID/Secret 未配置或错误  | 检查 `backend\.env` 中的微信配置                   |
| 后端启动报错"ModuleNotFoundError"| 依赖未安装                      | `pip install -r requirements.txt`                 |
| 前端编译报错"onShow is not exported"| 从 vue 而非 uni-app 导入     | 将 `import { onShow } from 'vue'` 改为 `from '@dcloudio/uni-app'` |
| 商品搜索结果为空                | 关键词过严或种子数据未加载       | 尝试短关键词；检查后端是否加载了 MySQL 种子数据      |
| 浏览历史不显示                  | API 字段不匹配（list vs items）  | 确认前端使用 `res.data?.items` 而非 `res.data?.list`|
| cpolar 地址每次重启变化         | 免费版限制                      | 登录 cpolar 官网可申请固定二级域名（付费）          |

---

> *本文档最后更新时间：2026-06-03*