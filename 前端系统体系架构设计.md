# 前端系统体系架构设计

## 1. 系统体系架构概述

### 1.1 技术栈选择
- **前端框架**: uni-app + Vue 3
- **状态管理**: Pinia
- **网络请求**: axios
- **样式处理**: SCSS
- **类型检查**: TypeScript
- **构建工具**: Vite

### 1.2 架构分层
```
┌─────────────────┐
│   表现层        │  UI组件、页面布局
├─────────────────┤
│   业务逻辑层    │  Vue组件、业务逻辑
├─────────────────┤
│   状态管理层    │  Pinia Store、全局状态
├─────────────────┤
│   服务层        │  API调用、数据处理
├─────────────────┤
│   工具层        │  工具函数、常量配置
└─────────────────┘
```

## 2. 系统功能结构（层次结构）

### 2.1 核心功能模块
- **用户管理**: 登录注册、个人资料管理
- **商品服务**: 搜索、分类、详情、比价、对比
- **个人中心**: 收藏、提醒、历史、设置

### 2.2 功能层次关系
```
一次买够小程序
├── 用户管理
│   ├── 微信登录
│   ├── 手机号登录
│   └── 用户信息管理
├── 商品服务
│   ├── 商品搜索
│   │   ├── 关键词搜索
│   │   ├── 智能搜索
│   │   └── 搜索历史
│   ├── 商品分类
│   ├── 商品详情
│   ├── 价格比价
│   │   ├── 多平台价格
│   │   ├── 价格趋势
│   │   └── 最佳购买建议
│   └── 商品对比
└── 个人中心
    ├── 我的收藏
    │   ├── 添加收藏
    │   ├── 删除收藏
    │   └── 收藏列表
    ├── 降价提醒
    │   ├── 设置提醒
    │   ├── 提醒列表
    │   └── 提醒通知
    ├── 浏览历史
    └── 设置中心
```

## 3. 系统用例时序图及说明

### 3.1 时序图说明

**用户搜索商品并比价时序图**展示了用户从搜索到比价的完整交互流程：

1. **用户输入阶段**: 用户在搜索框输入关键词
2. **前端处理阶段**: Vue组件接收输入，触发搜索逻辑
3. **状态管理阶段**: Pinia Store管理搜索状态和数据流
4. **API调用阶段**: 通过API服务调用后端接口
5. **数据处理阶段**: 后端返回数据，前端进行本地处理
6. **UI渲染阶段**: 组件根据状态更新界面显示

### 3.2 关键交互点
- **搜索触发**: 用户输入 → 防抖处理 → API调用
- **详情查看**: 点击商品 → 路由跳转 → 详情加载
- **价格比价**: 本地算法计算 → 排序展示 → 推荐生成
- **收藏操作**: 用户操作 → 状态更新 → 持久化存储

## 4. 复杂功能算法设计

### 4.1 商品比价算法

#### 算法流程
```pseudocode
function comparePrices(productId):
    // 1. 获取多平台价格数据
    platforms = api.getProductPlatforms(productId)

    // 2. 数据清洗和验证
    validPlatforms = validateAndClean(platforms)

    // 3. 价格排序算法
    sortedPlatforms = sortByPrice(validPlatforms)

    // 4. 计算价格统计
    priceStats = calculatePriceStats(sortedPlatforms)

    // 5. 平台可靠性评估
    platformScores = evaluatePlatformReliability(validPlatforms)

    // 6. 智能推荐算法
    recommendations = generateRecommendations(priceStats, platformScores)

    // 7. 生成比价报告
    report = createComparisonReport(sortedPlatforms, recommendations)

    return report
```

#### 核心算法逻辑

**价格排序算法**:
```javascript
function sortByPrice(platforms) {
    return platforms.sort((a, b) => {
        // 优先按价格升序
        if (a.price !== b.price) {
            return a.price - b.price;
        }
        // 价格相同时按平台评分降序
        return b.rating - a.rating;
    });
}
```

**平台可靠性评估**:
```javascript
function evaluatePlatformReliability(platform) {
    const weight = {
        userRating: 0.4,    // 用户评分权重
        salesVolume: 0.3,   // 销量权重
        responseTime: 0.2,  // 响应时间权重
        returnRate: 0.1     // 退货率权重
    };

    return platform.userRating * weight.userRating +
           platform.salesVolume * weight.salesVolume +
           platform.responseTime * weight.responseTime +
           platform.returnRate * weight.returnRate;
}
```

**智能推荐算法**:
```javascript
function generateRecommendations(priceStats, platformScores) {
    const recommendations = [];

    // 最佳价格推荐
    const bestPrice = priceStats.lowestPrice;
    recommendations.push({
        type: 'best_price',
        platform: bestPrice.platform,
        reason: '价格最低'
    });

    // 性价比推荐
    const bestValue = findBestValueRatio(priceStats, platformScores);
    recommendations.push({
        type: 'best_value',
        platform: bestValue.platform,
        reason: '性价比最高'
    });

    // 可靠性推荐
    const mostReliable = findMostReliable(platformScores);
    recommendations.push({
        type: 'most_reliable',
        platform: mostReliable.platform,
        reason: '平台最可靠'
    });

    return recommendations;
}
```

## 5. 面向对象方法类图设计

### 5.1 类图说明

#### 核心类层次
- **App类**: 应用入口，管理路由和全局状态
- **BaseComponent**: Vue组件基类，提供生命周期方法
- **PageComponent**: 页面组件类，继承基础组件
- **Store**: 状态管理基类
- **ApiService**: API服务基类，提供HTTP方法

#### 具体实现类
- **UserStore**: 用户状态管理
- **ProductStore**: 商品状态管理
- **FavoriteStore**: 收藏状态管理
- **AlertStore**: 提醒状态管理
- **ProductApi**: 商品相关API
- **UserApi**: 用户相关API

#### 数据模型类
- **Product**: 商品数据模型
- **Platform**: 平台数据模型

### 5.2 类关系说明

#### 继承关系
- `PageComponent` 继承 `BaseComponent`
- `UserStore`、`ProductStore` 等继承 `Store`
- `ProductApi`、`UserApi` 继承 `ApiService`

#### 依赖关系
- 页面组件依赖Store进行状态管理
- 页面组件依赖API服务进行数据获取
- 页面组件依赖工具类进行数据处理
- Store依赖API服务进行数据持久化

#### 组合关系
- `Product` 包含多个 `Platform` 对象
- `ProductStore` 管理 `Product` 对象的集合

### 5.3 设计原则

#### SOLID原则应用
- **单一职责**: 每个类都有明确的职责范围
- **开闭原则**: 通过继承和组合扩展功能
- **依赖倒置**: 高层模块不依赖低层模块的具体实现

#### 前端设计模式
- **状态管理模式**: 使用Pinia实现集中式状态管理
- **服务模式**: API调用封装为服务类
- **组件模式**: 可复用的UI组件设计
- **工厂模式**: 根据不同条件创建不同类型的对象

这个架构设计确保了代码的可维护性、可扩展性和可测试性，为项目的长期发展提供了坚实的技术基础。