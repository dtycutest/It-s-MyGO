# "一次买够" 第一阶段前端项目构建方案

## 📋 方案概览

**项目名称**：一次买够网购比价平台 - 微信小程序  
**阶段目标**：完成MVP（最小可行产品）核心功能开发  
**预计周期**：4周  
**技术栈**：uni-app + Vue 3 + Pinia + TypeScript + SCSS

---

## 🎯 第一阶段目标

### 核心功能交付清单
- ✅ 首页（搜索、分类、热搜、推荐）
- ✅ 商品搜索结果页（筛选、排序、列表）
- ✅ 商品详情页（价格对比、平台跳转）
- ✅ 登录/授权（微信登录）
- ✅ 用户基础状态管理

### 非功能目标
- 📱 完整的设计系统实现
- 🔌 API请求封装和错误处理
- 📊 基础状态管理架构
- ✅ 可扩展的项目结构

---

## 🗓️ 详细开发计划（4周）

### 第1周：项目初始化与基础设施

**目标**：搭建完整的项目架构和基础设施

#### 1.1 项目初始化
- [ ] 使用 HBuilderX 或 Vue CLI 创建 uni-app 项目
- [ ] 配置 TypeScript 支持
- [ ] 配置 SCSS/SASS 预处理器
- [ ] 配置 ESLint 和代码格式化规则
- [ ] 配置 Git 仓库和 .gitignore

#### 1.2 设计系统实现
- [ ] 实现全局样式变量文件 (`src/styles/variables.scss`)
  - 颜色系统（主色、辅助色、功能色）
  - 字体系统（标题、正文、价格）
  - 间距系统（xs/sm/md/lg/xl/xxl）
  - 圆角规范
- [ ] 实现通用工具类 (`src/styles/common.scss`)
- [ ] 实现重置样式和基础样式

#### 1.3 项目目录结构搭建
```
src/
├── pages/              # 页面
│   ├── index/         # 首页
│   ├── searchResults/ # 搜索结果页
│   ├── productDetail/ # 商品详情页
│   └── login/         # 登录页
├── components/        # 组件
│   ├── base/          # 基础组件
│   └── business/      # 业务组件
├── api/              # API模块
├── stores/           # Pinia状态管理
├── utils/            # 工具函数
├── styles/           # 全局样式
├── static/           # 静态资源
├── App.vue
├── main.ts
├── manifest.json
├── pages.json
└── uni.scss
```

#### 1.4 基础设施封装
- [ ] HTTP 请求封装 (`src/utils/request.ts`)
  - axios/uni.request 封装
  - 请求/响应拦截器
  - 错误处理
  - Token 自动刷新机制
- [ ] 本地存储工具 (`src/utils/storage.ts`)
- [ ] 认证工具 (`src/utils/auth.ts`)
- [ ] 格式化工具 (`src/utils/format.ts`)

#### 1.5 状态管理初始化
- [ ] 配置 Pinia
- [ ] 创建用户 Store (`src/stores/user.ts`)
- [ ] 创建商品 Store (`src/stores/product.ts`)
- [ ] 创建搜索 Store (`src/stores/search.ts`)

---

### 第2周：基础组件与首页开发

**目标**：完成核心基础组件和首页开发

#### 2.1 基础组件开发 (`src/components/base/`)
- [ ] Button 按钮组件
  - 主要按钮、次要按钮、文字按钮
  - 加载状态、禁用状态
- [ ] Input 输入框组件
  - 搜索框样式
  - 清除按钮
- [ ] Modal 弹窗组件
- [ ] Loading 加载组件
- [ ] Empty 空状态组件
- [ ] TabBar 标签栏组件

#### 2.2 业务组件开发 (`src/components/business/`)
- [ ] SearchBar 搜索栏组件
  - 搜索输入
  - 搜索历史
  - 清除功能
- [ ] CategoryNav 分类导航组件
  - 横向滚动
  - 选中状态
- [ ] BannerSwiper Banner轮播组件
  - 自动轮播
  - 指示器
- [ ] ProductCard 商品卡片组件
  - 商品图片
  - 价格显示
  - 平台徽章
  - 收藏按钮
- [ ] PriceLabel 价格标签组件
  - 不同尺寸
  - 降价标识
- [ ] PlatformBadge 平台徽章组件

#### 2.3 首页开发 (`src/pages/index/`)
- [ ] 页面布局实现
- [ ] 搜索栏集成
- [ ] 分类导航集成
- [ ] Banner轮播集成
- [ ] 热搜榜模块
  - 热搜数据获取
  - 点击搜索
- [ ] 推荐商品模块
  - 横向滚动卡片
- [ ] 页面交互逻辑

---

### 第3周：搜索结果页与商品详情页

**目标**：完成核心交易链路页面

#### 3.1 搜索结果页开发 (`src/pages/searchResults/`)
- [ ] 页面布局实现
- [ ] 顶部搜索栏（返回、修改搜索词）
- [ ] 筛选面板组件
  - 价格区间滑块
  - 平台多选
  - 品牌筛选
  - 重置/确定按钮
- [ ] 排序组件
  - 价格升/降序
  - 销量排序
  - 评分排序
- [ ] 商品列表
  - 列表渲染
  - 下拉加载更多
  - 上拉刷新
- [ ] 筛选和排序逻辑
- [ ] API集成

#### 3.2 商品详情页开发 (`src/pages/productDetail/`)
- [ ] 页面布局实现
- [ ] 商品图片轮播
- [ ] 商品基本信息
  - 标题
  - 价格区间
  - 最优平台标识
- [ ] 价格对比表格
  - 多平台价格展示
  - 平台信息
  - 销量、评分
- [ ] 价格走势图（可选，第一阶段可简化）
- [ ] 商品描述和规格
- [ ] 操作按钮
  - 收藏按钮
  - 降价提醒
  - 去购买（跳转原平台）
- [ ] API集成

#### 3.3 搜索功能完善
- [ ] 搜索历史管理
- [ ] 搜索关键词联想
- [ ] 搜索历史记录保存

---

### 第4周：登录、用户中心与联调测试

**目标**：完成用户系统和整体测试

#### 4.1 登录模块开发 (`src/pages/login/`)
- [ ] 微信登录按钮
- [ ] 授权流程实现
  - 获取 code
  - 调用后端登录接口
  - 保存 token 和用户信息
- [ ] 登录状态持久化
- [ ] 自动登录逻辑

#### 4.2 用户中心基础版 (`src/pages/userCenter/`)
- [ ] 用户信息展示
  - 头像
  - 昵称
- [ ] 功能入口（占位，第二阶段完善）
  - 我的收藏
  - 降价提醒
  - 浏览历史
- [ ] 设置入口
- [ ] 退出登录功能

#### 4.3 收藏功能基础实现
- [ ] 收藏API集成
- [ ] 收藏状态管理
- [ ] 收藏按钮交互

#### 4.4 整体联调与测试
- [ ] 完整流程测试
  - 搜索 → 查看详情 → 收藏
- [ ] API联调
- [ ] 错误处理测试
- [ ] 边界情况处理
- [ ] 真机测试
- [ ] 性能优化

---

## 🏗️ 技术架构设计

### 1. API层设计

**文件结构**：
```
src/api/
├── index.ts          # API配置
├── auth.ts           # 认证相关
├── product.ts        # 商品相关
├── user.ts           # 用户相关
├── favorite.ts       # 收藏相关
└── types.ts          # TypeScript类型定义
```

**关键API封装**：
```typescript
// src/api/product.ts
import request from '@/utils/request'

export interface SearchParams {
  keyword: string
  page?: number
  page_size?: number
  category_id?: number
  min_price?: number
  max_price?: number
  platform?: string
  sort?: string
}

export interface ProductItem {
  product_id: string
  title: string
  image_url: string
  min_price: number
  max_price: number
  platforms: Array<{
    platform_name: string
    price: number
  }>
}

export const productApi = {
  search: (params: SearchParams) => {
    return request.get('/products/search', { params })
  },
  
  detail: (productId: string) => {
    return request.get(`/products/${productId}`)
  },
  
  hotWords: (limit = 10) => {
    return request.get('/search/hot-words', { params: { limit } })
  },
  
  categories: (parentId = 0) => {
    return request.get('/products/categories', { params: { parent_id: parentId } })
  }
}
```

### 2. 状态管理设计

**User Store** (`src/stores/user.ts`)：
```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { authApi } from '@/api/auth'

export const useUserStore = defineStore('user', () => {
  const token = ref('')
  const userInfo = ref<User | null>(null)
  const isLoggedIn = computed(() => !!token.value)

  const setToken = (newToken: string) => {
    token.value = newToken
    uni.setStorageSync('token', newToken)
  }

  const login = async (code: string, rawData: string, signature: string) => {
    const res = await authApi.login({ code, raw_data, rawData, signature })
    setToken(res.access_token)
    userInfo.value = {
      user_id: res.user_id,
      nick_name: res.nick_name,
      avatar_url: res.avatar_url
    }
    return res
  }

  const logout = () => {
    token.value = ''
    userInfo.value = null
    uni.removeStorageSync('token')
  }

  return {
    token,
    userInfo,
    isLoggedIn,
    setToken,
    login,
    logout
  }
})
```

**Product Store** (`src/stores/product.ts`)：
```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { productApi, type ProductItem } from '@/api/product'

export const useProductStore = defineStore('product', () => {
  const searchResults = ref<ProductItem[]>([])
  const currentProduct = ref<ProductDetail | null>(null)
  const hotWords = ref<HotWord[]>([])
  const loading = ref(false)
  const pagination = ref({ page: 1, page_size: 20, total: 0 })

  const searchProducts = async (params: SearchParams) => {
    loading.value = true
    try {
      const res = await productApi.search(params)
      if (params.page === 1) {
        searchResults.value = res.list
      } else {
        searchResults.value = [...searchResults.value, ...res.list]
      }
      pagination.value = res.pagination
      return res
    } finally {
      loading.value = false
    }
  }

  const getProductDetail = async (productId: string) => {
    const res = await productApi.detail(productId)
    currentProduct.value = res
    return res
  }

  const getHotWords = async (limit = 10) => {
    const res = await productApi.hotWords(limit)
    hotWords.value = res
  }

  return {
    searchResults,
    currentProduct,
    hotWords,
    loading,
    pagination,
    searchProducts,
    getProductDetail,
    getHotWords
  }
})
```

### 3. 请求封装设计

```typescript
// src/utils/request.ts
import { useUserStore } from '@/stores/user'

const BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api/v1'

const request = {
  async get<T>(url: string, config?: RequestConfig): Promise<T> {
    return this._request<T>('GET', url, config)
  },

  async post<T>(url: string, data?: any, config?: RequestConfig): Promise<T> {
    return this._request<T>('POST', url, { ...config, data })
  },

  async _request<T>(method: string, url: string, config?: RequestConfig): Promise<T> {
    const userStore = useUserStore()
    
    const header: any = {
      'Content-Type': 'application/json',
      'X-Client-Version': '1.0.0',
      'X-Client-Platform': 'miniapp'
    }
    
    if (userStore.token) {
      header['Authorization'] = `Bearer ${userStore.token}`
    }

    try {
      const res = await uni.request({
        url: BASE_URL + url,
        method,
        header,
        data: config?.data || config?.params,
        timeout: 30000
      })

      const { code, message, data } = res.data as ApiResponse<T>

      if (code === 0) {
        return data
      } else if (code === 401002 || code === 401003) {
        // Token过期，尝试刷新或重新登录
        userStore.logout()
        uni.navigateTo({ url: '/pages/login/login' })
        throw new Error(message)
      } else {
        uni.showToast({ title: message, icon: 'none' })
        throw new Error(message)
      }
    } catch (error) {
      if (!error.message.includes('network')) {
        uni.showToast({ title: '网络错误', icon: 'none' })
      }
      throw error
    }
  }
}

export default request
```

---

## 🎨 UI组件规范

### 组件设计原则
1. **单一职责** - 每个组件只做一件事
2. **可复用性** - 组件参数化，支持多种场景
3. **TypeScript优先** - 完整的类型定义
4. **插槽设计** - 支持灵活的内容扩展
5. **事件驱动** - 通过 emit 与父组件通信

### 核心组件清单

| 组件名 | 路径 | 功能 | 优先级 |
|--------|------|------|--------|
| Button | components/base/Button | 基础按钮 | P0 |
| SearchBar | components/business/SearchBar | 搜索栏 | P0 |
| ProductCard | components/business/ProductCard | 商品卡片 | P0 |
| PriceLabel | components/business/PriceLabel | 价格标签 | P0 |
| PlatformBadge | components/business/PlatformBadge | 平台徽章 | P0 |
| BannerSwiper | components/business/BannerSwiper | Banner轮播 | P1 |
| FilterPanel | components/business/FilterPanel | 筛选面板 | P1 |
| CategoryNav | components/business/CategoryNav | 分类导航 | P1 |

---

## 📊 第一阶段里程碑验收标准

### 功能验收
- [ ] 用户可以搜索商品
- [ ] 可以查看商品详情和多平台价格
- [ ] 可以筛选和排序搜索结果
- [ ] 可以微信登录
- [ ] 可以收藏商品

### 技术验收
- [ ] 代码符合 TypeScript 规范
- [ ] 组件复用率达到 60% 以上
- [ ] API 错误处理完善
- [ ] 页面加载时间 < 2s
- [ ] 无严重 bug

### 质量验收
- [ ] 完成单元测试覆盖核心逻辑
- [ ] 完成真机测试（至少 3 种机型）
- [ ] 完成代码评审
- [ ] 编写开发文档

---

## 🚀 快速启动指南

### 环境准备
```bash
# 1. 安装 HBuilderX
# 下载地址: https://www.dcloud.io/hbuilderx.html

# 2. 或使用 Vue CLI
npm install -g @vue/cli
npm install -g @dcloudio/vue-cli-plugin-uni
```

### 项目创建
```bash
# 使用 Vue CLI 创建
vue create -p dcloudio/uni-preset-vue my-project

# 进入项目
cd my-project

# 安装依赖
npm install

# 安装 Pinia
npm install pinia

# 安装 TypeScript（如需要）
npm install typescript @types/uni-app -D
```

### 开发命令
```bash
# 微信小程序开发
npm run dev:mp-weixin

# 预览
npm run dev:h5
```

---

## 📋 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| API接口延期交付 | 高 | 提前与后端团队对齐接口时间，准备 Mock 数据 |
| 微信审核问题 | 中 | 提前准备审核材料，留出审核缓冲时间 |
| 性能问题 | 中 | 做好图片懒加载、分页加载等优化 |
| 团队时间不足 | 高 | 优先保证 P0 功能，P1 功能可延后到第二阶段 |

---

## 🎯 第二阶段规划（预览）

- 完善用户中心（收藏、降价提醒、浏览历史）
- 价格走势图
- 商品对比功能
- 分享功能
- 推送通知
- 性能优化和埋点

---

**文档版本**：v1.0  
**创建日期**：2026年4月26日  
**维护者**：前端开发团队
