# 接口逻辑检查与更新方案

## 📋 检查结果总结

经过对 `openapi.json` 的详细检查，发现我们之前的方案**基本正确**，但有一些细节需要调整和完善。

---

## 🔧 需要调整的内容

### 1. API 路径修正

| 原方案路径 | 实际 OpenAPI 路径 | 说明 |
|-----------|------------------|------|
| `/search/hot-words` | `/search/hot-words` | ✓ 正确 |
| `/products/categories` | `/products/categories` | ✓ 正确 |
| `/recommendations/personalized` | `/recommendations/personalized` | ✓ 正确 |
| `/users/favorites` | `/users/favorites` | ✓ 正确 |
| `/users/price-alerts` | `/users/price-alerts` | ✓ 正确 |
| `/users/browse-history` | `/users/browse-history` | ✓ 正确 |

### 2. 数据模型补充

#### 新增平台枚举
```typescript
// 平台代码枚举
type PlatformCode = 'taobao' | 'jingdong' | 'pinduoduo' | 'amazon' | 'other';

// 平台名称枚举
type PlatformName = '淘宝' | '京东' | '拼多多' | '亚马逊' | '其他';
```

#### Platform 数据模型
```typescript
interface Platform {
  platform_id: number;
  platform_name: PlatformName;
  platform_code: PlatformCode;
  price: number;
  original_price?: number;
  discount_rate?: number;
  sales_volume?: number;
  seller_name?: string;
  seller_rating?: number;
  seller_id?: string;
  product_url: string;
  in_stock: boolean;
  stock_quantity?: number;
  update_at: string;
}
```

### 3. 新增关键接口

#### 浏览历史接口
```typescript
// 添加浏览历史
POST /users/browse-history
{
  "product_id": "SKU20250315001"
}

// 获取浏览历史
GET /users/browse-history?page=1&page_size=20

// 清除浏览历史
DELETE /users/browse-history
```

#### 搜索记录接口
```typescript
// 获取搜索记录
GET /users/search-records?page=1&page_size=20
```

#### 个性化推荐接口
```typescript
// 获取个性化推荐
GET /recommendations/personalized?page=1&page_size=20
```

#### 事件上报接口
```typescript
// 上报用户行为
POST /analytics/events
{
  "event_type": "search" | "product_click" | "add_favorite" | "remove_favorite" | "alert_set" | "purchase_redirect",
  "product_id": "SKU20250315001",
  "platform": "taobao",
  "timestamp": "2025-03-24T10:30:00Z",
  "metadata": {
    "keyword": "iPhone 15"
  }
}
```

### 4. 收藏接口调整

移除收藏时使用 `favorite_id` 而不是 `product_id`：
```typescript
// 正确
DELETE /users/favorites/{favorite_id}

// 更新收藏备注
PUT /users/favorites/{favorite_id}
{
  "notes": "618打折再买"
}
```

### 5. 错误码处理完善

需要处理所有定义的错误码：
- `400001` - 参数缺失
- `400002` - 参数类型错误
- `400003` - 参数值范围错误
- `401001` - Token缺失
- `401002` - Token过期
- `401003` - Token无效
- `401004` - 签名验证失败
- `403001` - 无访问权限
- `403002` - 资源禁止访问
- `404001` - 用户不存在
- `404002` - 商品不存在
- `429001` - 搜索请求频率限制
- `429002` - API调用频率限制
- `500001` - 数据库操作失败
- `500002` - 第三方服务异常
- `500003` - 爬虫服务异常

---

## 📝 更新后的 API 层设计

### 完整的 types.ts
```typescript
// src/api/types.ts

// ============ 基础类型 ============
export interface ApiResponse<T = any> {
  code: number;
  message: string;
  data: T;
}

export interface Pagination {
  page: number;
  page_size: number;
  total: number;
  total_pages: number;
  has_next: boolean;
  has_prev: boolean;
}

export interface PaginatedResponse<T> {
  list: T[];
  pagination: Pagination;
}

// ============ 平台相关 ============
export type PlatformCode = 'taobao' | 'jingdong' | 'pinduoduo' | 'amazon' | 'other';
export type PlatformName = '淘宝' | '京东' | '拼多多' | '亚马逊' | '其他';

export interface Platform {
  platform_id: number;
  platform_name: PlatformName;
  platform_code: PlatformCode;
  price: number;
  original_price?: number;
  discount_rate?: number;
  sales_volume?: number;
  seller_name?: string;
  seller_rating?: number;
  seller_id?: string;
  product_url: string;
  in_stock: boolean;
  stock_quantity?: number;
  update_at: string;
}

// ============ 商品相关 ============
export interface Product {
  product_id: string;
  title: string;
  category_id: number;
  category_name: string;
  image_url: string;
  images?: string[];
  platforms: Platform[];
  min_price: number;
  max_price: number;
  price_diff: number;
  best_platform: string;
  description?: string;
  specs?: Record<string, string>;
  created_at: string;
  updated_at: string;
}

export interface SearchParams {
  keyword: string;
  page?: number;
  page_size?: number;
  category_id?: number;
  min_price?: number;
  max_price?: number;
  platform?: string;  // 多个用逗号分隔
  sort?: 'price_asc' | 'price_desc' | 'sales_desc' | 'rating_desc';
}

export interface Category {
  category_id: number;
  name: string;
  parent_id: number;
  children?: Category[];
}

export interface HotWord {
  keyword: string;
  count: number;
  rank: number;
}

// ============ 用户相关 ============
export interface User {
  user_id: number;
  openid: string;
  nick_name: string;
  avatar_url: string;
  gender?: number;
  province?: string;
  city?: string;
  created_at: string;
  updated_at: string;
  vip_level: number;
  vip_expire_at?: string;
}

export interface LoginRequest {
  code: string;
  raw_data: string;
  signature: string;
}

export interface LoginResponse {
  access_token: string;
  refresh_token: string;
  expires_in: number;
  user_id: number;
  openid: string;
  nick_name?: string;
  avatar_url?: string;
}

export interface UpdateUserRequest {
  nick_name?: string;
  avatar_url?: string;
  gender?: number;
}

// ============ 收藏相关 ============
export interface Favorite {
  favorite_id: number;
  user_id: number;
  product_id: string;
  product_title: string;
  product_image: string;
  product_min_price: number;
  notes?: string;
  created_at: string;
  updated_at: string;
}

export interface AddFavoriteRequest {
  product_id: string;
  notes?: string;
}

export interface UpdateFavoriteRequest {
  notes: string;
}

// ============ 降价提醒相关 ============
export interface PriceAlert {
  alert_id: number;
  user_id: number;
  product_id: string;
  product_title: string;
  target_price: number;
  current_price: number;
  is_triggered: boolean;
  triggered_at?: string;
  is_enabled: boolean;
  platform_filter?: string[];
  created_at: string;
  updated_at: string;
}

export interface CreatePriceAlertRequest {
  product_id: string;
  target_price: number;
  platform_filter?: string[];
}

export interface UpdatePriceAlertRequest {
  target_price?: number;
  is_enabled?: boolean;
}

// ============ 浏览历史 ============
export interface BrowseHistory {
  history_id: number;
  user_id: number;
  product_id: string;
  product_title: string;
  product_image: string;
  viewed_at: string;
}

export interface AddBrowseHistoryRequest {
  product_id: string;
}

// ============ 搜索记录 ============
export interface SearchRecord {
  record_id: number;
  user_id: number;
  keyword: string;
  search_count: number;
  created_at: string;
  updated_at: string;
}

// ============ 事件上报 ============
export type EventType = 'search' | 'product_click' | 'add_favorite' | 'remove_favorite' | 'alert_set' | 'purchase_redirect';

export interface TrackEventRequest {
  event_type: EventType;
  product_id?: string;
  platform?: string;
  timestamp: string;
  metadata?: Record<string, any>;
}
```

### 完整的 API 模块

```typescript
// src/api/index.ts
import request from '@/utils/request';
import type {
  ApiResponse, PaginatedResponse,
  Product, SearchParams, Category, HotWord,
  User, LoginRequest, LoginResponse, UpdateUserRequest,
  Favorite, AddFavoriteRequest, UpdateFavoriteRequest,
  PriceAlert, CreatePriceAlertRequest, UpdatePriceAlertRequest,
  BrowseHistory, AddBrowseHistoryRequest,
  SearchRecord,
  TrackEventRequest,
} from './types';

// ============ 认证 API ============
export const authApi = {
  login(data: LoginRequest): Promise<ApiResponse<LoginResponse>> {
    return request.post('/auth/login', data);
  },
  
  refresh(refresh_token: string): Promise<ApiResponse<{ access_token: string; refresh_token: string; expires_in: number }>> {
    return request.post('/auth/refresh', { refresh_token });
  },
  
  logout(): Promise<ApiResponse<null>> {
    return request.post('/auth/logout');
  }
};

// ============ 商品 API ============
export const productApi = {
  search(params: SearchParams): Promise<ApiResponse<PaginatedResponse<Product>>> {
    return request.get('/products/search', { params });
  },
  
  detail(product_id: string): Promise<ApiResponse<Product>> {
    return request.get(`/products/${product_id}`);
  },
  
  categories(parent_id = 0): Promise<ApiResponse<Category[]>> {
    return request.get('/products/categories', { params: { parent_id } });
  }
};

// ============ 搜索 API ============
export const searchApi = {
  hotWords(limit = 10): Promise<ApiResponse<HotWord[]>> {
    return request.get('/search/hot-words', { params: { limit } });
  }
};

// ============ 用户 API ============
export const userApi = {
  getProfile(): Promise<ApiResponse<User>> {
    return request.get('/users/profile');
  },
  
  updateProfile(data: UpdateUserRequest): Promise<ApiResponse<User>> {
    return request.put('/users/profile', data);
  }
};

// ============ 收藏 API ============
export const favoriteApi = {
  list(page = 1, page_size = 20): Promise<ApiResponse<PaginatedResponse<Favorite>>> {
    return request.get('/users/favorites', { params: { page, page_size } });
  },
  
  add(data: AddFavoriteRequest): Promise<ApiResponse<Favorite>> {
    return request.post('/users/favorites', data);
  },
  
  update(favorite_id: number, data: UpdateFavoriteRequest): Promise<ApiResponse<Favorite>> {
    return request.put(`/users/favorites/${favorite_id}`, data);
  },
  
  remove(favorite_id: number): Promise<ApiResponse<null>> {
    return request.delete(`/users/favorites/${favorite_id}`);
  }
};

// ============ 降价提醒 API ============
export const priceAlertApi = {
  list(page = 1, page_size = 20, status: 'all' | 'triggered' | 'untriggered' = 'all'): Promise<ApiResponse<PaginatedResponse<PriceAlert>>> {
    return request.get('/users/price-alerts', { params: { page, page_size, status } });
  },
  
  create(data: CreatePriceAlertRequest): Promise<ApiResponse<PriceAlert>> {
    return request.post('/users/price-alerts', data);
  },
  
  update(alert_id: number, data: UpdatePriceAlertRequest): Promise<ApiResponse<PriceAlert>> {
    return request.put(`/users/price-alerts/${alert_id}`, data);
  },
  
  delete(alert_id: number): Promise<ApiResponse<null>> {
    return request.delete(`/users/price-alerts/${alert_id}`);
  }
};

// ============ 浏览历史 API ============
export const browseHistoryApi = {
  list(page = 1, page_size = 20): Promise<ApiResponse<PaginatedResponse<BrowseHistory>>> {
    return request.get('/users/browse-history', { params: { page, page_size } });
  },
  
  add(data: AddBrowseHistoryRequest): Promise<ApiResponse<null>> {
    return request.post('/users/browse-history', data);
  },
  
  clear(): Promise<ApiResponse<null>> {
    return request.delete('/users/browse-history');
  }
};

// ============ 搜索记录 API ============
export const searchRecordApi = {
  list(page = 1, page_size = 20): Promise<ApiResponse<PaginatedResponse<SearchRecord>>> {
    return request.get('/users/search-records', { params: { page, page_size } });
  }
};

// ============ 推荐 API ============
export const recommendationApi = {
  personalized(page = 1, page_size = 20): Promise<ApiResponse<PaginatedResponse<Product>>> {
    return request.get('/recommendations/personalized', { params: { page, page_size } });
  }
};

// ============ 分析 API ============
export const analyticsApi = {
  track(data: TrackEventRequest): Promise<ApiResponse<null>> {
    return request.post('/analytics/events', data);
  }
};
```

---

## 🎯 更新后的开发计划

### 第1周 - 基础设施（不变）
- 项目初始化
- 设计系统实现
- API 层搭建（使用更新后的接口）
- State 管理

### 第2周 - 基础组件与首页（微调）
- 组件开发（不变）
- 首页开发（新增：浏览历史记录、事件上报）

### 第3周 - 搜索结果页与商品详情页（新增功能）
- 搜索结果页（不变）
- 商品详情页（新增：浏览历史上报、事件上报）

### 第4周 - 登录、用户中心与联调测试（大幅扩展）
- 登录模块（不变）
- 用户中心（新增：收藏列表、降价提醒、浏览历史、搜索记录）
- 完整联调

---

## ✅ 检查清单

| 项目 | 状态 | 说明 |
|------|------|------|
| API 路径正确性 | ✅ 已修正 | 所有路径已确认 |
| 数据模型完整性 | ✅ 已补充 | Platform、事件类型等已补充 |
| 错误码处理 | ⏳ 待实现 | 需要在 request 层处理所有错误码 |
| 新增接口覆盖 | ✅ 已覆盖 | 浏览历史、事件上报等已添加 |
| TypeScript 类型安全 | ✅ 已完善 | 完整的类型定义 |

---

## 📌 关键要点

1. **搜索关键词是必填的**：`GET /products/search` 必须包含 `keyword` 参数
2. **平台筛选**：使用英文代码（taobao, jingdong 等），多个用逗号分隔
3. **排序选项**：price_asc, price_desc, sales_desc, rating_desc
4. **收藏删除**：使用 favorite_id，不是 product_id
5. **事件上报**：建议在关键操作时调用，用于数据分析和推荐优化
6. **Token 刷新机制**：需要完善 token 过期时的自动刷新逻辑
