# 一次买够爬虫模块联调对接说明

本文档用于和前端、后端同学对接 `onebuy_crawler / 一次买够` 爬虫模块。

## 1. 当前项目进度

当前爬虫模块已经完成以下能力：

1. 京东商品搜索采集已跑通。
   - 支持 Playwright 打开真实浏览器页面。
   - 支持监听京东异步价格接口。
   - 支持从 DOM 商品卡片兜底提取商品。
   - 已验证可以采集 `iPhone 15`、`蓝牙耳机`、`机械键盘`、`显示器`、`充电宝`、`华为 手机` 等关键词。

2. 淘宝商品搜索采集已实现。
   - 支持淘宝 Network JSON/JSONP 响应解析。
   - 支持页面状态对象和 DOM 商品卡片兜底提取。
   - 淘宝更容易触发登录/安全验证，当前不会破解验证，而是支持人工登录/验证后继续采集。

3. MySQL 入库链路已完成。
   - 商品主表：`products`
   - 平台报价表：`platform_offers`
   - 价格历史表：`price_history`
   - 原始抓取记录表：`raw_crawl_records`
   - 后台补采任务表：`crawl_tasks`

4. 后台任务机制已完成。
   - 查不到数据时可以创建 `crawl_tasks`。
   - 后台脚本可以处理补采任务。
   - 京东默认优先，淘宝需要显式指定 `--platform taobao`。

5. 查询支持分页。
   - `jobs.query_products` 支持 `--page` 和 `--page-size`。

6. 联调保障已完成。
   - 支持导入内置种子数据。
   - 支持导出已有数据库缓存为 JSON，给其他同学导入。

## 2. 环境配置

在 PowerShell 中进入项目目录：

```powershell
cd D:\study_file\Junior\ComprehensiveComputerProject\onebuy_crawler
```

设置 MySQL 环境变量：

```powershell
$env:MYSQL_HOST="127.0.0.1"
$env:MYSQL_PORT="3306"
$env:MYSQL_USER="root"
$env:MYSQL_PASSWORD="123456"
$env:MYSQL_DATABASE="onebuy"
$env:CRAWLER_ENABLE_MYSQL="1"
```

初始化数据库表：

```powershell
.\.venv\Scripts\python.exe -m jobs.init_schema
```

## 3. 京东采集命令

京东是当前主力采集平台，优先用于联调。

采集单个关键词：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --keyword "蓝牙耳机" --force --rounds 1 --task-limit 1 --pages 1 --item-limit 20 --timeout 30 --open-strategy direct-first
```

采集多个常用关键词：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --use-default-keywords --rounds 2 --task-limit 4 --pages 1 --item-limit 20 --timeout 30 --open-strategy direct-first
```

直接调用浏览器采集入口：

```powershell
.\.venv\Scripts\python.exe -m jobs.browser_capture_search --platform jd --keyword "iPhone 15" --pages 1 --limit 20 --timeout 30 --open-strategy direct-first --headless
```

当前京东推荐优先使用直接搜索结果页。若出现 `jd_search_redirected_to_home`、`jd_access_too_frequent`、验证码或风控页，后台任务会停止本轮京东采集并延迟重试，避免连续请求导致后续关键词全部失败。

查询结果：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "蓝牙耳机" --page 1 --page-size 10
```

## 4. 淘宝采集命令

淘宝搜索页更容易触发登录或安全验证。首次使用建议先准备淘宝浏览器登录态：

```powershell
.\.venv\Scripts\python.exe -m jobs.prepare_browser_profile --platform taobao
```

在打开的浏览器中登录淘宝并完成必要验证，回到终端按 Enter 保存登录态。

然后采集淘宝关键词：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --platform taobao --keyword "蓝牙耳机" --force --rounds 1 --task-limit 1 --pages 1 --item-limit 20 --timeout 35
```

如果中途淘宝要求验证，浏览器会停住。完成验证后回到终端按 Enter，程序会继续采集。

后台静默失败记录模式：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --platform taobao --keyword "蓝牙耳机" --force --rounds 1 --task-limit 1 --pages 1 --item-limit 20 --timeout 35 --headless
```

## 5. 查询与分页

查询第一页，每页 10 条：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "蓝牙耳机" --page 1 --page-size 10
```

查询第二页：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "蓝牙耳机" --page 2 --page-size 10
```

查不到时创建京东补采任务：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "显示器" --enqueue-missing
```

处理补采任务：

```powershell
.\.venv\Scripts\python.exe -m jobs.run_crawl_tasks --limit 5 --platform jd --pages 1 --item-limit 20 --timeout 30 --open-strategy direct-first
```

## 6. 给其他同学提供什么

对接时建议提供以下内容：

1. 本项目代码目录。
   - `onebuy_crawler`
   - `requirements.txt`
   - `README.md`
   - 本文档 `联调对接说明.md`

2. MySQL 配置。

```text
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=onebuy
CRAWLER_ENABLE_MYSQL=1
```

3. 数据库初始化命令。

```powershell
.\.venv\Scripts\python.exe -m jobs.init_schema
```

4. 推荐联调采集命令。

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --use-default-keywords --rounds 2 --task-limit 4 --pages 1 --item-limit 20 --timeout 30
```

5. 查询验证命令。

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "iPhone 15" --page 1 --page-size 10
```

6. 可选：导出已有数据给同学。

```powershell
.\.venv\Scripts\python.exe -m jobs.export_seed_data --use-default-keywords --output data/team_seed_products.json
```

同学导入：

```powershell
.\.venv\Scripts\python.exe -m jobs.import_seed_data --seed-file data/team_seed_products.json --use-default-keywords --only-missing
```

不要直接分享自己的淘宝或京东登录态目录、Cookie、账号信息。

## 7. 后端对接建议

后端不建议在用户搜索请求中同步启动爬虫。推荐流程：

```text
前端发起搜索
后端先查 MySQL 的 products 和 platform_offers
有数据：直接返回商品列表
无数据：写入 crawl_tasks，返回“数据补充中”
后台脚本低频处理 crawl_tasks
采集成功后再次搜索即可返回数据
```

后端主要读取：

```text
products
platform_offers
price_history
```

其中：

- `products`：商品聚合信息。
- `platform_offers`：不同平台报价、店铺、商品链接。
- `price_history`：历史价格记录。

## 8. 已知问题与边界

1. 京东采集已较稳定，但仍可能遇到访问频繁、验证码、搜索重定向。
2. 淘宝更容易触发登录或安全验证，需要人工登录/验证后再采集。
3. 程序不会破解验证码，不绕过安全验证。
4. 如果采集失败，会写入 `raw_crawl_records` 并保存调试快照到：

```text
output/browser_debug/
```

5. 如果数据库已有旧任务，建议指定关键词时加 `--force`，确保本次任务立即执行。

## 9. 推荐验收流程

1. 初始化数据库：

```powershell
.\.venv\Scripts\python.exe -m jobs.init_schema
```

2. 采集京东数据：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --keyword "iPhone 15" --force --rounds 1 --task-limit 1 --pages 1 --item-limit 20 --timeout 30
```

3. 查询京东结果：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "iPhone 15" --page 1 --page-size 10
```

4. 准备淘宝登录态：

```powershell
.\.venv\Scripts\python.exe -m jobs.prepare_browser_profile --platform taobao
```

5. 采集淘宝数据：

```powershell
.\.venv\Scripts\python.exe -m jobs.auto_crawl --platform taobao --keyword "蓝牙耳机" --force --rounds 1 --task-limit 1 --pages 1 --item-limit 20 --timeout 35
```

6. 查询淘宝结果：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "蓝牙耳机" --page 1 --page-size 10
```

看到商品列表，即表示爬虫采集、清洗、去重、入库、查询链路完整可用。

## 10. 本次数据交付包

当前已经生成统一联调用数据包：

```text
data/team_seed_products.json
```

本次数据情况：

```text
products: 79
platform_offers: 89
price_history: 89
导出记录数: 89
```

默认关键词覆盖情况：

```text
iPhone 15: 22
华为 手机: 3
小米 手机: 17
蓝牙耳机: 3
机械键盘: 16
显示器: 3
充电宝: 18
```

其中大部分数据来自本次真实京东采集；`华为 手机`、`蓝牙耳机`、`显示器` 在本次采集时触发京东验证/重定向，已用项目种子缓存补齐，保证前后端联调时每个默认关键词都有可查数据。

给其他同学时，至少发送：

```text
data/team_seed_products.json
联调对接说明.md
```

同学导入方式：

```powershell
cd D:\study_file\Junior\ComprehensiveComputerProject\onebuy_crawler

$env:MYSQL_HOST="127.0.0.1"
$env:MYSQL_PORT="3306"
$env:MYSQL_USER="root"
$env:MYSQL_PASSWORD="123456"
$env:MYSQL_DATABASE="onebuy"
$env:CRAWLER_ENABLE_MYSQL="1"

.\.venv\Scripts\python.exe -m jobs.init_schema
.\.venv\Scripts\python.exe -m jobs.import_seed_data --seed-file data/team_seed_products.json
```

导入后验证：

```powershell
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "iPhone 15" --page 1 --page-size 10
.\.venv\Scripts\python.exe -m jobs.query_products --keyword "蓝牙耳机" --page 1 --page-size 10
```

如果同学数据库里已经有数据，不想重复覆盖，可以使用：

```powershell
.\.venv\Scripts\python.exe -m jobs.import_seed_data --seed-file data/team_seed_products.json --use-default-keywords --only-missing
```
